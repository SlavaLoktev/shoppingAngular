export class Attribute {
    attributeId: number;
    attributeName: string;

    constructor(attributeId: number, attributeName: string) {
        this.attributeId = attributeId;
        this.attributeName = attributeName;
    }
}
